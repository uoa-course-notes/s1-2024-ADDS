#include <vector>


/**
* Return the k-th largest element in an unsorted vector of integers. 
*
* @param[in] values := the unsorted vector of integers 
* @param[in] k := the desired rank of the largest element
* @paragraph Description Paragraph title?
*
*
* @param[out] k_largest_element >= 0  
* @return k_largest 
*
*/

/**
 * @brief The discrete-time Fourier time convolution propert states that 
 * @f{eqnarray*}
 * x(n) * y(n) \Leftrightarrow X{e^{j/omega}} )Y(e^) \\
 * another equation here
 * @f}
 *  // Didn't work
 * Fit a model of the form @f $y = ax + b$@f
*
 * 
 * @param values 
 * @param k 
 * @return int 
 */



int kth_largest(std::vector<int> values, int k){
    /* Informal doc:
    - values := an unsorted vector of integers.

    = k := the k-th largest element in values.
    
    */


    return 1;
}